import { SalaryfilterPipe } from './salaryfilter.pipe';

describe('SalaryfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SalaryfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
